<form method="post" action="{{ path('app_activation_delete', {'id': activation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activation.id) }}">
    <button class="btn">Delete</button>
</form>
